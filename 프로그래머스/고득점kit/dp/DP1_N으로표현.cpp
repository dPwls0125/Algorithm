#include <stdlib.h>
#include <string>
#include <vector>
#include <map>
#include <algorithm>
#include <iostream>
#include <cmath>
using namespace std;

//bottop-up
int solution(int N, int number) {
    int answer = 0;
    return answer;
}

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);
    int answer = 0;
    int n;
    cin >> n;
    return answer;
}